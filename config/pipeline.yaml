shopee:
  base_url: https://shopee.vn
  search_path: /api/v4/search/search_items
  limit_per_page: 60
  request_delay_sec: 1

paths:
  duckdb: data/tiki_scraped_data_raw.duckdb
  raw: data/raw
  rejected: data/rejected

warehouse:
  type: bigquery_sandbox
  project_id: my-sandbox-project
  dataset: shopee_warehouse
